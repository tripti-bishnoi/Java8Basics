import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class MyStack {

	
	int[] a = {1,2,3,4,5};
	
	List<Integer> l = Arrays.asList(a);
	
	
	id, time, json data
	
	select 1 json_data from table_name
	where date(time) == date(now())-30;
	
	l.stream()
	.forEach(sys)
	
	private ArrayList<Object> list; 
	
	public MyStack(){
		list = new ArrayList<Object>();
	}
	
	public void push(Object val){
		list.add(val);
	}
	
	
	public Object pop(){
		return list.get(list.size()-1);
		
	}
	
	public boolean checkPallinderom(String data, int start, int end) {

		if (data.charAt(end) != data.charAt(start))

			return false;

		return true && checkPallinderom(data, start++, end--);

	}

	
	/*
	 * two arrays
	 * diff lengths
	 * 
	 * 
	 */
	
	public int[] mergeArrays(int[] a, int[] b ){
		
		Arrays.sort(a);
		
		
	}
}


amortizationperiod = 20years
rate = 7%
ltv = 10
